- content = capture do
  .site-content-container
    .site-btn-notice-container.d-flex.flex-sm-row.justify-content-sm-between.align-items-sm-center.flex-column.justify-content-between.align-items-center
      .desc
        코리아 챌린지의 새 소식과
        %br.d-block.d-sm-none
        공지사항을 알려드립니다.
      - if policy(Story).new?
        %a.btn.btn-white.btn-outline-primary{ href: new_story_path } 글쓰기
    - if @stories.any?
      .site-line-list-container.mb-5
        .site-card-list
          = render @stories
        = paginate @stories
    - elsif @stories.total_count <= 0
      .site-basic-contents
        .text-center.text-muted.mb-5
          새 소식과 공지사항을 준비 중입니다.

= render 'layouts/story', content: content
